# מדריך למתחילים: Claude Code
## איך להיות טוב בזה מהר

---

## מה זה Claude Code?

Claude Code הוא כלי AI שרץ ישירות בטרמינל שלך.
במקום לפתוח דפדפן ולהדביק קוד, אתה כותב **בשורת הפקודה** והוא עובד ישירות על הפרויקט שלך.

```
מסורתי:  אתה → copy code → ChatGPT → copy back → paste → שגיאה → חזור
Claude Code: אתה → כתוב בטרמינל → הוא עושה הכל
```

---

## התקנה (דקה אחת)

```bash
npm install -g @anthropic-ai/claude-code
claude
```

זהו. אתה בפנים.

---

## 5 הפקודות שתשתמש בהן 90% מהזמן

### 1. פשוט לדבר איתו
```bash
claude
```
פותח מצב שיחה. תכתוב מה שתרצה בעברית או אנגלית.

### 2. שאלה מהירה בלי לפתוח שיחה
```bash
claude -p "מה הפונקציה הזו עושה?"
```

### 3. לתת לו קובץ לקרוא
```bash
claude -p "הסבר לי את הקובץ הזה" < myfile.py
```

### 4. המשך שיחה קודמת
```bash
claude --continue
```

### 5. לצאת
```
/exit  או  Ctrl+C
```

---

## איך לכתוב פרומפטים טובים

### רע vs טוב

| רע | טוב |
|---|---|
| "תקן את הקוד" | "הפונקציה `login()` מחזירה None במקום True, תקן" |
| "תוסיף פיצ'ר" | "תוסיף ולידציה על שדה email בטופס ההרשמה" |
| "יש באג" | "כשאני לוחץ שמור, מופיעה שגיאה `TypeError: Cannot read properties of undefined`" |

### הנוסחה:
```
[מה הבעיה/מה אתה רוצה] + [איפה בקוד] + [מה הציפייה שלך]
```

**דוגמה:**
```
תוסיף טיפול בשגיאות לפונקציה fetchUser בקובץ api.js
כשה-API מחזיר 404, צריך להציג הודעה "משתמש לא נמצא"
```

---

## פקודות Slash — הכלים החבויים

בתוך שיחה עם Claude Code, יש פקודות מיוחדות:

| פקודה | מה היא עושה |
|---|---|
| `/help` | רשימת כל הפקודות |
| `/clear` | נקה את ההיסטוריה (שיחה חדשה) |
| `/compact` | דחס את ההיסטוריה לחסוך tokens |
| `/cost` | כמה tokens השתמשת עד עכשיו |
| `/model` | החלף מודל (Opus/Sonnet/Haiku) |
| `/review` | בקש קוד ריוויו על השינויים הנוכחיים |

---

## דגלים שימושיים

```bash
# הפעל Claude Code בתיקייה ספציפית
claude --add-dir /path/to/project

# הפעל עם מודל מסוים
claude --model claude-opus-4-6

# מצב ללא אישורים (אוטומטי לחלוטין - זהירות!)
claude --dangerously-skip-permissions
```

---

## דוגמאות מעשיות

### דוגמה 1: הבנת קוד זר
```
אתה: "הסבר לי מה הקובץ auth/middleware.js עושה בשפה פשוטה"

Claude Code: קורא את הקובץ ומסביר בעברית פשוטה
```

### דוגמה 2: כתיבת טסטים
```
אתה: "כתוב unit tests לפונקציה calculateDiscount בקובץ pricing.js
      השתמש ב-Jest, כסה edge cases של 0%, 100% ומספרים שליליים"

Claude Code: כותב את הטסטים ומוסיף אותם לקובץ
```

### דוגמה 3: Refactor
```
אתה: "הפונקציה processOrders ב-orders.js ארוכה מדי (200 שורות)
      פצל אותה ל-3 פונקציות קטנות ושמור על אותה התנהגות"

Claude Code: מפצל, מוסיף לקוד, ושואל אם לאשר
```

### דוגמה 4: Debug
```
אתה: "הריצו את npm test - יש שגיאה בטסט הזה:
      Expected: 42, Received: undefined
      הטסט הוא ב-utils.test.js שורה 34"

Claude Code: מוצא את הבאג ומציע תיקון
```

---

## תהליך עבודה מומלץ

```
1. פתח טרמינל בתיקיית הפרויקט
2. כתוב: claude
3. תאר מה אתה רוצה לעשות היום
4. תן לו לעבוד — הוא ישאל אם לא ברור
5. בדוק את השינויים (git diff)
6. אשר או בקש תיקון
```

---

## טיפים שיחסכו לך זמן

### טיפ 1: תן הקשר בהתחלה
```
"אני בונה אפליקציית React עם TypeScript ו-Firebase.
עובד על מודול ההתחברות. צריך להוסיף login עם Google."
```

### טיפ 2: /compact לשיחות ארוכות
אחרי הרבה הודעות, כתוב `/compact` — הוא יסכם ויחסוך tokens.

### טיפ 3: CLAUDE.md לפרויקט
צור קובץ `CLAUDE.md` בתיקיית הפרויקט עם הוראות קבועות:
```markdown
# הוראות לפרויקט
- שפת תכנות: TypeScript
- בדיקות: Jest
- סגנון: Airbnb ESLint
- כל פונקציה חדשה חייבת JSDoc
```
Claude Code קורא אותו **אוטומטית** בכל שיחה.

### טיפ 4: git commit אחרי כל שינוי גדול
```bash
# אחרי שClause Code שינה קבצים
git diff        # ראה מה השתנה
git add .
git commit -m "feat: הוסף login עם Google"
```

### טיפ 5: בקש הסברים
```
"עשית את השינוי — עכשיו הסבר לי למה בחרת בגישה הזו"
```
זה הכי מהיר ללמוד.

---

## שגיאות נפוצות של מתחילים

| שגיאה | פתרון |
|---|---|
| פרומפט כללי מדי | הוסף קונטקסט: קובץ, שורה, שגיאה ספציפית |
| לא בודקים את השינויים | תמיד `git diff` לפני commit |
| שיחה ארוכה מדי בלי /compact | השתמש ב-/compact כל ~20 הודעות |
| לא מגדירים CLAUDE.md | צור אותו — חוסך הסברים חוזרים |
| מבקשים הרבה דברים בבת אחת | שאלה אחת בכל פעם, צעד צעד |

---

## רמות מיומנות

### מתחיל (שבוע 1)
- [ ] הפעל claude בטרמינל
- [ ] בקש הסבר על קובץ קיים
- [ ] בקש תיקון לבאג פשוט

### בינוני (שבוע 2-3)
- [ ] השתמש ב-CLAUDE.md
- [ ] כתוב פרומפטים מפורטים
- [ ] בקש כתיבת טסטים

### מתקדם (חודש 1+)
- [ ] תהליך עבודה שלם: plan → code → test → commit
- [ ] שימוש ב-hooks להפעלה אוטומטית
- [ ] Multi-agent לפרויקטים מורכבים

---

## סיכום: 3 הכללים הכי חשובים

1. **פרומפט ספציפי** — ככל שתהיה יותר מדויק, כך התוצאה תהיה טובה יותר
2. **צעד אחד בכל פעם** — אל תבקש 5 דברים בבת אחת
3. **בדוק את השינויים** — `git diff` הוא חבר שלך

---

*Claude Code Documentation: https://docs.anthropic.com/en/docs/claude-code*
